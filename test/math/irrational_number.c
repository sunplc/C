/**
 * 打印1除以1,2,3...
 * 打印出其中的有限小数、无限循环小数和无限不循环小数
 * 自然数 1,2,3,4,5...
 * 有理数 整数、有限小数和无限循环小数
 * 无理数，也称为无限不循环小数，不能写作两整数之比。
 */

#include <stdio.h>

int main(void)
{
    int i = 1;
    for (; i < 100; i++) {
        printf("1 / %4d = %.18f\n", i, 1.0/i);
    }
    return 0;
}
