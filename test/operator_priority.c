#include <stdio.h>

int main()
{
	// 二元运算符 + 和 - 具有相同的优先级,他们的优先级比 * / %低,
	// 而 * / % 的优先级又比一元运算符 + 和 - 低,算数运算符是从左向右进行结合的.
	int a = 10;

	//a = 1 + (a++) + 1 + (a);
	//a = 1 + (a) + 1 + (a++);
	
	//a = -a % 3;
	a = -a + 8 * 3;

	printf("%d \n", a);

	return 0;
}

